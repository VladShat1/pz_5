#Описать функцию SortInc3(А, В, С), меняющую содержимое переменных А, В, С таким
# образом, чтобы их значения оказались упорядоченными по возрастанию
# (А, B, C -вещественные параметры, являющиеся одновременно входными и выходными).
# С помощью этой функции упорядочить по возрастанию два данных набора из трех чисел:
# (Аi, Вi, Сi) и (A2, B2, C2).


try:
    lst = []

    def SortInc3(A, B, C):
        if C < A < B:



    A = float(input('Введите вещественное число A: '))
    B = float(input('Введите вещественное число B: '))
    C = float(input('Введите вещественное число C: '))

    SortInc3(A, B, C)
    lst.sort()
    print(lst)

except:
    print('Ошибка')

